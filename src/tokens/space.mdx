import { Meta } from '@storybook/addon-docs/blocks'
import { SpacePaletteDisplay } from '../../.storybook/components/space-palette'

<Meta title="Tokens/Spacing" />

# Spacing

Tailwind v4 uses a single `--spacing` multiplier (4px base unit).
All spacing values are computed as `calc(var(--spacing) * n)`.

## Shadow Scale

<SpacePaletteDisplay spaceRange="shadow" />

